import { Card, CardHeader, CardTitle } from '@/components/ui/card';

export const DangerZone = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle></CardTitle>
      </CardHeader>
    </Card>
  );
};
